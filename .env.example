# Discord bot credentials (from https://discord.com/developers/applications)
# Bot -> Token
DISCORD_BOT_TOKEN=your-bot-token-here
# General Information -> Application ID (also "Client ID")
DISCORD_APPLICATION_ID=your-application-id-here

# Path to the SQLite file that stores per-guild configuration.
# Use an absolute path or a volume mount in production so it survives restarts.
DATABASE_PATH=./data/bot.sqlite

# --- PostHog Code bridge (required) ---------------------------------------
# Shared secret used as a bearer token in BOTH directions: the bot sends it
# when forwarding /ph interactions to PostHog, and requires it on inbound calls
# to the actions API. Must match the value configured on the PostHog side.
POSTHOG_DISCORD_SHARED_SECRET=your-shared-secret-here
# host:port the bot's inbound actions API binds to (PostHog -> bot). Use
# 0.0.0.0 to accept connections from off-box, or 127.0.0.1 when fronted by a
# reverse proxy / tunnel.
BOT_ACTIONS_BIND=0.0.0.0:8080
# Dev only: override the PostHog app host the bridge forwards to (otherwise it's
# derived from the guild's analytics region). Point at a tunneled local PostHog.
# POSTHOG_BRIDGE_BASE_URL=http://127.0.0.1:8000

# Optional: how often the server_snapshot event fires, in hours (default 1).
# One event per server per run, so hourly is cheap; raise it to sample less often,
# at the cost of no longer being able to see presence by hour of day.
# SNAPSHOT_INTERVAL_HOURS=1

# Optional: how often the member_roster sweep runs, in hours (default 24). Each
# run emits one event per member, so lower values cost proportionally more.
# ROSTER_INTERVAL_HOURS=24

# --- PostHog Support tickets (optional) -----------------------------------
# Turns watched forum posts into PostHog Support tickets.
#
# Creating a ticket goes through the same endpoint the browser SDK uses, because
# the REST route answers 405 ("use posthog.conversations.sendMessage()"). It
# authenticates with the project's PUBLIC conversations token plus an Origin on
# the project's Support domain allowlist — no personal API key involved.
#
# NOTE: that widget endpoint is not part of PostHog's documented API and can
# change without notice. Creates then fail loudly in the logs; Discord is
# unaffected.
#
# Public conversations token: Settings -> Support (safe to expose, like phc_).
# POSTHOG_CONVERSATIONS_TOKEN=
# Origin sent with widget calls. MUST be listed under Settings -> Support ->
# allowed domains, or PostHog answers 403 "Origin not allowed". It is only a
# header, so nothing needs to be hosted there.
# POSTHOG_CONVERSATIONS_ORIGIN=https://discord-bot.example.com
# Optional: capture host the widget endpoint lives on (default us.i.posthog.com).
# POSTHOG_TICKETS_CAPTURE_HOST=https://us.i.posthog.com

# Optional, ONLY needed to append replies to an existing ticket (the documented
# REST route). Creating tickets does not use these. This is a PERSONAL API key
# with the `ticket:write` scope — real write authority, so it stays in env and
# never in SQLite. Create one at https://us.posthog.com/settings/user-api-keys
# POSTHOG_TICKETS_API_KEY=
# POSTHOG_TICKETS_PROJECT_ID=
# Optional: app host for the REST tickets API (default https://us.posthog.com).
# POSTHOG_TICKETS_HOST=https://us.posthog.com
